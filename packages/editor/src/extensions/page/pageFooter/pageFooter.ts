/**
 * `PageFooter`。
 *
 * 见 `../pageHeader/pageHeader.ts`：一份实现，两侧共用。
 *
 * `PageFooter`.
 *
 * See `../pageHeader/pageHeader.ts`: one implementation, two sides.
 */

export { PageFooter } from "../utils/furniture";
